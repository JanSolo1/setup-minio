{
  "name": "setup-minio",
  "version": "1.0.0",
  "description": "GitHub Action to setup MinIO Client (mc)",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "lint": "eslint . --ext .ts",
    "prepack": "npm run build"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/io": "^1.1.3",
    "@actions/tool-cache": "^2.0.2"
  },
  "devDependencies": {
    "@types/node": "^20.19.22",
    "typescript": "^5.9.3"
  }
}
